# Findesk Daily Pulse

Static public dashboard for the daily Findesk update.

Replace `reports/current.md` with the next update and push the change to `main`. GitHub Pages republishes the dashboard automatically.
